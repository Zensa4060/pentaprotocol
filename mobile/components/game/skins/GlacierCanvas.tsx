/**
 * Deprecated — Glacier (and every grid) now renders the verbatim web canvas
 * via WebView (see [webCanvas.ts] / [CanvasWebView]). Kept as an empty module
 * so no stray import pulls in `react-native-skia`.
 */

export {};
