/**
 * Deprecated — the Skia engine was replaced by the WebView approach, which
 * runs the verbatim web `<canvas>` code (see [webCanvas.ts] / [CanvasWebView]).
 * Intentionally empty so no module imports `react-native-skia`.
 */

export {};
