[phases.setup]
nixPkgs = ["python313", "python313Packages.pip", "rustc", "cargo", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["pip install maturin && pip install rust_engine/"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 8080"
