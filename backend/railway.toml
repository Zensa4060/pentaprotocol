[build]
builder = "nixpacks"
buildCommand = ".venv/bin/pip install maturin && .venv/bin/pip install rust_engine/"

[deploy]
startCommand = ".venv/bin/uvicorn main:app --host 0.0.0.0 --port 8080"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
