[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && pip install maturin && pip install rust_engine/"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port 8080"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
